# requirements.txt

# === Core Functionality ===
python-docx >= 1.1.0
PyPDF2 >= 3.0.0
ollama >= 0.2.0
pydantic >= 2.5.0
jobspy >= 0.29.0
geopy >= 2.4.0
rich >= 13.0.0
pandas >= 2.0.0
PyYAML >= 6.0        # For reading config.yaml
Jinja2 >= 3.1.0      # For prompt templating

# === Added/Ensured Dependencies ===
aiohttp >= 3.9.0     # Preferred async HTTP client for Ollama interactions
# httpx >= 0.27.0    # Alternative async client if preferred

# === Transitive Dependencies (Usually installed by above) ===
# beautifulsoup4
# playwright >= 1.40
# requests
# charset-normalizer # Often needed